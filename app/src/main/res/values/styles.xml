<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

<!--    自定义主题，主要是为了去掉标题栏-->
    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>  //去掉Title
    </style>

<!--    UserFragment页面下方设置选项框最外层的样式-->
    <style name="UserFrame">
        <item name="android:orientation">vertical</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">0dp</item>
        <item name="android:layout_marginRight">0dp</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:padding">1px</item>
    </style>

<!--    UserFragment页面下方设置选项框最外层共用的样式-->
    <style name="BaseLayout">
        <item name="android:orientation">horizontal</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

<!--    分割线-->
    <style name="Separator">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">4dp</item>
    </style>

<!--    UserFragment压面下方设置选项框的TextView的样式-->
    <style name="UserSettingText">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/userTextColor</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
    </style>

<!--    UserFragment页面下方设置列表的样式-->
    <style name="SettingListLayout" parent="BaseLayout">
        <item name="android:background">@drawable/background_change</item>
    </style>
    
<!--    OptionFragment页面按钮的样式-->
<!--    https://blog.csdn.net/weixin_34405354/article/details/86054797-->
    <style name="ImageButtonLayout">
        <item name="android:layout_weight">0.5</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
    
</resources>
